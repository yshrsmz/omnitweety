import { defineBackground } from 'wxt/utils/define-background'

// Placeholder entrypoint for WXT migration scaffold (PR1).
// The real implementation moves here in PR2.
export default defineBackground(() => {})
